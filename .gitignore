# Python (monólito legado)
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.venv/
venv/
instance/
*.db

# Segredos
.env
.env.local
.env.*.local
# `.env.prod.example` é o molde e fica versionado. Qualquer outra variação
# (`.env.prod.test`, usada para subir a stack de produção na máquina local)
# carrega valor real ou de teste e não deve entrar no repositório — que, além
# de tudo, é público.
.env.prod.*
!.env.prod.example

# Node / TypeScript
node_modules/
dist/
build/
coverage/
*.tsbuildinfo
.eslintcache

# Expo
.expo/
web-build/

# Sistema
.DS_Store
Thumbs.db

# Dumps da base legada: dados reais (hashes de senha, e-mails). Nunca versionar.
backups/
