services:
  hope_desk:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    restart: unless-stopped
    networks:
      - web

networks:
  web:
    external: true

